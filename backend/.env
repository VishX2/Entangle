PORT=8000
NODE_ENV=development
# Required: replace YOUR_PASSWORD with your real PostgreSQL password for user "postgres".
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# If postgres has no password (some local installs): postgresql://postgres@localhost:5432/entangle_db
# Create the DB first: in psql run CREATE DATABASE entangle;  or use pgAdmin.
DATABASE_URL=postgresql://postgres:Silva183290@localhost:5432/entangle_db
JWT_SECRET=change-this-to-a-long-random-secret
