{
  "name": "backend",
  "version": "1.0.0",
  "description": "Node.js/Express API with **Prisma** ORM. Schema: `prisma/schema.prisma` (aligned with `entangle_db.sql`).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.19.0"
  }
}
